# Source & config (compiled output in out/ is enough)
src/**
tsconfig.json
CLAUDE.md

# Dev tooling
.vscode/**
.vscode-test/**
.claude/**

# Dependencies — only bundle declared runtime deps
node_modules/**
!node_modules/@iarna
!node_modules/@iarna/toml
!node_modules/@iarna/toml/**

# Compiled tests (not part of the extension)
out/test/**

# Build artifacts & packaging
*.vsix
package-lock.json
dist/**
build/**
scripts/**

# Git & misc
.git/**
.gitignore

# Screenshots & contribution assets
contrib/**

# Quickstart doc (if it exists)
vsc-extension-quickstart.md

# Source maps
**/*.map

# TypeScript source in out (if any .ts ends up there)
out/**/*.ts
